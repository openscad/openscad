// This is a single line comment

/* this 
   is a
   multiline 
   comment
*/

//functions
abs() concat() lookup() looksup() concatination() intersection_for

//transformations
linear_extrude() extrudes() scales() scale()
scale () 
mirror multmatrix multimatrix 

//booleans
true false union difference intersection()

//keywords
 echo(version=version()); 
echo ( version = version () ) ; 
echos secho params param include() use() interface  param 
see

//operators
<= >= != 
== &&

//modifiers
# function () {
	something is here 
}
 
% function2() {
	new things
	again
}

! module name(){
	name can be reused 
}

