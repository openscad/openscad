{
  "name": "wasm-test",
  "version": "1.0.0",
  "private": true,
  "description": "Local browser smoke-test pages for the WASM build (dev-only).",
  "scripts": {
    "postinstall":
      "node -e \"require('fs').mkdirSync('vendor',{recursive:true});require('fs').copyFileSync('node_modules/three/build/three.min.js','vendor/three.min.js')\""
  },
  "dependencies": {
    "three": "0.128.0"
  }
}
