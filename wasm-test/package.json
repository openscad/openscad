{
  "name": "wasm-test",
  "version": "1.0.0",
  "private": true,
  "description": "Local browser smoke-test pages for the WASM build (dev-only).",
  "scripts": {
    "postinstall":
      "node -e \"const fs=require('fs');fs.mkdirSync('vendor',{recursive:true});for(const f of ['three.module.min.js','three.core.min.js'])fs.copyFileSync('node_modules/three/build/'+f,'vendor/'+f)\""
  },
  "dependencies": {
    "three": "0.185.0"
  }
}
